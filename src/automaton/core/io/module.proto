syntax = "proto3";

message bin2hex {
  message request {
    bytes input = 1;
  }

  message response {
    bytes output = 1;
  }
}

message hex2bin {
  message request {
    bytes input = 1;
  }

  message response {
    bytes output = 1;
  }
}
